package io.codelex.enums.practice;

public class CardinalPointsTest {
    public static void main(String[] args) {
        CardinalPoints.printDirection(CardinalPoints.North);
        CardinalPoints.printDirection(CardinalPoints.South);
        CardinalPoints.printDirection(CardinalPoints.East);
        CardinalPoints.printDirection(CardinalPoints.West);
    }
}
